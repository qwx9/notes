#!/bin/rc
rfork n
if(! ~ $#* 1){
	echo usage: $0 path
	exit usage
}
note/mkdir $1
if(! ~ $status '')
	exit mkdir
cd $noteroot || exit
p=$notepath^.md
# either this or modify /sys/lib/plumbing to start $editor
# even if file doesn't exist
touch $p
B $p
